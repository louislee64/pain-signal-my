# Same-origin path; Nitro proxies it to the API service (see nuxt.config.ts).
NUXT_PUBLIC_API_BASE_URL=/api/v1
# Where the API is reachable from inside the container network.
NUXT_API_INTERNAL_URL=http://api:8000
